[build-system]
requires = ["setuptools>=64", "wheel", "setuptools_scm>=8,<10", "pybind11>=2.10,<3", "cmake>=3.18,<4"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/fdtdz_jax/fdtdz_jax_version.py"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
